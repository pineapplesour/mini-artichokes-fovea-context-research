# Frontend

The frontend/design contract is `FRONTEND_REQUIREMENTS.md`.

Current runtime files still live in `../web` and `../native`. Do not move them without a tested migration PR.

Design teams should treat this folder as the governance root for frontend work.

