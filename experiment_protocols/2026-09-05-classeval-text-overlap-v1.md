# Full ClassEval-Pro300 code-output comparison, v1

This is a NEW full300-task development run, not a continuation or score
replacement of the native-agent partial campaign. At the native feasibility
check,25 completed calls included10 timeouts and no original composition
trials. Do not spend the remaining native campaign budget merely to repeat
this inability to provide two valid partial programs. Preserve its partial
results and every initiated call as development costs; no full-benchmark
claim from that incomplete run. No source-only throughput improvement is a
scientific contribution of the paper.

Use the SAME author ClassEval-Pro release/dataSHA, complete300 inventory,
unchanged official tests/evaluator and disclosed291/300 reference outcome.
No subset, repaired gold, selected retry, old answer or old model feedback is
used. All generation starts fresh with only its original skeleton.

## Benchmark-native output and fixed scientific contrast

The approved Codex account still runs gpt-5.6-luna/medium with a120-second cap.
The model now returns one complete Python code block, without tools, instead
of conducting a filesystem-editing episode. The same outer filesystem
isolation is retained. Shell/code-mode tools are disabled, read-only tool
sandboxing applies, and any non-text tool event invalidates the call. Require
normal completion and parseable code; timeouts remain failures. Extract a
code block mechanically and apply only the author's unchanged staticmethod
preprocessing. Do not repair model text on the host.

The OpenAI Docs skill informed the supported configuration/CLI checks, not a
new model selection or account change. Sources:
https://learn.chatgpt.com/docs/config-file/config-reference
https://developers.openai.com/codex/cli/reference
Existing local IsolatedCodexAdapter is reused with process-local command
transformation; none of its source or the old experiment outputs is changed.

Generate A for each task; generate an independent, identical-prompt B once
for every A failure. Preserve A full passes for every system and skip B for
those tasks. Run the frozen exclusive-helper-union source space for BOTH
uniform-random and overlap-ranked policies, each capped at16 new programs.
No semantic ranking, source-space bounds or test gates are changed here.

Compare these complete final systems:

- Ordinary: best actual A/B source followed, if unresolved, by one general
  execution-feedback repair call.
- Generic crossover: random source mixture selection followed, if unresolved,
  by the SAME general feedback-repair prompt.
- Overlap: overlap-ranked mixture selection followed, if unresolved, by the
  SAME general feedback-repair prompt.

All repairs use the same no-tool code-output model/effort/cap and receive the
original skeleton, both original parents and their bounded case feedback,
and the selected starting program with its feedback. No special overlap repair
instruction or ledger. Passing states remain unchanged. Each final system
uses at most3 logical model calls per task; physical parent calls are shared.
When repair prompts are BYTE-IDENTICAL, make one physical call and share its
result, charging one logical call to each relevant system. This reuse rule is
fixed BEFORE outcomes, saves calls and prevents random differences between
identical systems from being misattributed to the source operator. Different
repair inputs get separate calls. No winner selection among rerolls.

Report Plain, adaptive verified best-of-up-to-two, both source-only policies,
and all3 repaired final systems over ALL300. Report realized tokens/time and
model/test calls; equal limits are not claims of exactly equal realized cost.
Ordinary repair is a strong additional control, not a full canonical Reflexion
replication. Random crossover is an ablation/comparator, not renamed novelty.
Source-only success is insufficient for promotion. The final overlap system
must beat BOTH repaired generic controls with appropriately adjusted paired
statistics and then be checked in fresh full-inventory sessions. This run
remains development, including the benchmark's known reference limitations.
