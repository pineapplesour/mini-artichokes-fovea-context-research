# Pre-repair, pre-scoring transport amendment: probe IDs only

The original probe-author call completed safely in 273.583 seconds, changed no
source, and produced47 unique task rows (43 proposed executable,4 unavailable).
It used the exact official exercise slugs but omitted the uniform `aider-java/`
namespace. All47 slugs map bijectively to the frozen manifest; no task is absent.
The original strict-ID gate rejected it, and both dependent repair calls were
never started. There are no repair outcomes, probe/candidate comparative scores,
or new official solution scores available at this amendment.

To avoid another model call for a pure transport label, retain the rejected raw
packet and create a separate canonical copy with ONLY taskId changed through
the unique manifest-ID suffix mapping. Reject if any mapping is ambiguous,
missing, duplicated, or changes inventory. Every other JSON field, including
incorrect probe code, status, basis, and reason, remains identical. No model
output or score is inferred, repaired, omitted, or selected. Both final arms
receive the byte-identical canonical packet and the same receipt.

This supersedes only the strict full-ID-encoding portion of the shared-packet
gate for this development pilot. It does NOT relax complete-task coverage,
agent completion, source immutability, probe independence, resource settings,
or prohibition on result-led replacements. Preserve raw/canonical hashes and
the mapping. Report this pre-scoring amendment; do not describe the pilot as
an unchanged original protocol or a strict new-task confirmation.
