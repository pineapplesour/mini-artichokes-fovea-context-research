# Overlap after feedback repair: same three-call history, no fresh solver calls

Hypothesis: feedback repair creates new correct behavior but can lose earlier
correct behavior. Combining the pre-repair code with its repair may be more
useful than combining two initial draws, which showed strongly correlated
failures in the initial textv2 prefix. This is a distinct exposed-track
development hypothesis, not a retroactive change to the ongoing full run.

Use only the shared ordinary history A/B plus its one ordinary repair. Do
not use repairs from the generic or overlap source-policy branches. Preserve
an already fully passing ordinary final program unchanged for BOTH policies.
For every unresolved task, choose the best actual A/B parent by full pass,
then passing-case count, stable A-first ties, and pair it with the actual
ordinary repair candidate. Invalid repair executions stay invalid; do not
reinterpret timeout artifacts. The intact best observed final remains the
baseline. All model calls already occurred in textv2; charge the complete
ordinary history to every derived system, with no added model calls.

Apply the existing helper-union operator unchanged to this pair. Random and
overlap-ranked source search get the SAME candidate space and16-program cap,
with the same14-slot bound and unchanged isolated official evaluation. The
only policy contrast is ranking. Report all official300 tasks if complete;
an exact all-completed-prefix snapshot is a DIAGNOSTIC only, never a selected
benchmark result. Record original artifact hashes, exact ordinary repair
identity, new executable trials and outcomes. Do not merge source-only
predictions or passing-case unions into whole-problem scores.

The equal-three-call comparators are the original ordinary system and the
post-repair random-crossover system. Disclose additional verifier work versus
ordinary and matched verifier caps between crossover policies. A successful
post-hoc development contrast requires fresh full-inventory repetition before
promotion. No new model campaign or manuscript claim is authorized by merely
implementing this replay.
