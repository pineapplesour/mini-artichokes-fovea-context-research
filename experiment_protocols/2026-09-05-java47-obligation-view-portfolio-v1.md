# Expectation-aligned route versus a repeated-generic verifier ensemble

## Before-outcome freeze and original user objective

At2026-09-04 23:56:29UTC, neither `obligation_view_v1_case_local` nor
`obligation_view_v1_obligation_overlap` had a result.json or system-result.json.
This sibling-system contrast is fixed before those outcomes and before the
additional control call. It is separate from the already frozen two-arm
representation comparison and does not change either live execution.

The user wants an overlap-CONTAINING system to beat a generic ensemble, not a
requirement that every standalone overlap route dominate every generic route.
Test both questions distinctly. Do not promote an overlap-free improvement as
fulfilling the intended contribution.

## Identical five-execution system budgets

Shared original prefix: Plain P, requirement-Graph G and ordinary repair R.
Shared final route A: `obligation_view_v1_case_local`.

- Heterogeneous system: P/G/R + A + `obligation_view_v1_obligation_overlap`.
- Homogeneous comparator: P/G/R + A + `obligation_view_v1_case_local_b`.

Each system contains FIVE logical whole-track agent executions. The shared
prefix is reused, not regenerated. The physical new final executions across
these sibling studies are THREE: A, overlap and B. Only B is additional to the
currently running two-arm study. No independent-probe author is included.

B is a fresh execution of the EXACT case-local normalized feedback, generic
repair instruction, original R seed, P/G/R sources, raw feedback, model/account,
effort,900-second cap and180-second completion reserve. It receives no A or
overlap outputs, ledgers, scores, feedback, patches or notes. Its view bytes and
stable prompt template must match A. All three canonical failure-record hashes
must match. Across representations, only repair-view.json differs among staged
evidence files; original raw evidence and source hashes must match.

## Scheduling and selection

Wait for the current driverPID1924528/session2157 to finish its unchanged A→O
sequence. Start B only if both original system reports exist and their normal
completion/source/evidence/ledger/materialization gates succeeded. This gate
uses eligibility, never scores or signs. If either is invalid, retain it and
close this sibling comparison without B or an eligible primary conclusion.
If B is invalid, retain it without replacement or post-score source repair.
No other experimental model call runs concurrently.

Terminal source priority for each system is its differing route (O or B), then
A, R, G, P. For each task, select the first complete passing state in that fixed
order; if none passes, retain the differing route as a failed fallback. Copy
only declared solution files, materialize each complete47-task system and run
all original official evaluator commands. Require the resulting vector to match
the declared verifier union. No oracle patch mixing within a task.

## Fixed endpoints and claims

Primary system endpoint: full47-task materialized heterogeneous-minus-homogeneous
pass difference. Separately report the original standalone O-minus-A comparison,
all candidate scores, rescue/harm IDs, actual agent time/tokens and all eligibility
deviations. Five executions are whole-track CLI runs, not five API requests or
five independent prefixes. Conditional task-discordance tests, if shown, are
descriptive of realized executions, not independent-session confirmation.

A useful system gain warrants fresh, separately frozen paired-system replication
and transfer to complete official tracks. No universal, cross-model or untouched-
task superiority is inferred from this Java development contrast. Do not compare
the heterogeneous union only against its own A constituent and call the gain an
equal-budget architecture effect. The comparator is the complete P/G/R/A/B
verified ensemble under the same promotion rule and logical execution count.

Analyzer: `tools/analyze_aider_probe_portfolio.py --study obligation_view_v1`.
Use its original probe_v1 mode only for that older six-execution study.
