# ClassEval-Pro overlap-repair tail (development candidate)

Status: APPROVED CONDITIONALLY by root for one development execution only after the existing full300 parent and contrast replay completed, full analyzer passed without diagnostic mode, the prefix300 dry-run integrity gate passed with U<=50, and the preflight/run-root gate passed; this remains a development experiment, not fresh confirmation.

1. Production first validates the ordinary prefix as the exact official 300-task inventory, frozen data SHA, terminal parent contract/result, and every A/B/R receipt. A partial parent is rejected.
2. Before any call, claim one new non-overlapping sibling run root (existing, equal, nested, or ancestor roots are rejected), write the contract, and persist progress after every task. Unexpected call/evaluator/IO errors preserve progress and error metadata and end INCOMPLETE; no retry or fabricated complete300 result. If the disk itself cannot accept writes, those error/progress guarantees are impossible.
3. Contract records parent root, parent contract/result hashes, tail protocol/text/evaluator/helper hashes, Luna/medium/120-second settings, and a common prefix map of ordinary call paths, selected-source SHA, and logical prefix calls.
4. The ordinary selected source and bounded observed feedback are common E/D/O inputs. Feedback is validation-assisted evidence, not a leakage claim: observed code/expected-value failure messages are retained, deterministic case order is used, and the aggregate fatal+message text (not JSON metadata) is <=24,000 characters with a truncation marker; no row test, solution, gold, or raw test source is serialized.
5. An ordinary pass stops E/D/O identically. Otherwise each arm independently calls A and B, then C: exactly three new model calls, A/B peer-isolated, logical prefix (1–3)+tail (3) <=6.
6. Method labels use skeleton AST, then ordinary-source AST; if both fail, all arms use one whole-program E fallback and overlap is false. For matched raw `cases[*].methods`, rank failure anchor by `(f/(f+p), f, original-order)` and move it to `k=n//2`; absent/mismatched traces retain order. The hint is common routing metadata, not causal evidence.
7. Every E/D/O A/B/C applies the same AST canonicalization -> `base.evaluation_source` -> evaluation path. E/C may repair the whole program; D/O A/B project only assigned existing methods onto the ordinary parent, preserving all outside statements. New helpers/imports are C-only.
8. Results distinguish planned overlap from active overlap: actual replaced methods and changed methods are recorded separately; A/B/C bridge-body SHA values are recorded as provenance (null when absent), without requiring equality and without calling hashes a causal/use proof. O is active only when valid A/B/C contain the target bridge method body, and stopped/dry/invalid cases are inactive. No other arm's outputs or mutation lists enter common hints.
9. The automatic raw evaluations from `text_runner.call` are discarded for C/selection and counted separately; canonical evaluations share a six-suite-evaluation cap (raw+canonical, invalid candidates are not canonically evaluated). Selection is fixed prefix/A/B/C by `(pass, passed-case-count)`.
10. Prompts use the same contract-check checklist and code-only rule (one Python block; each `def` signature on one line with `self`/`cls` on that line); no generated decision ledger is requested or reconciled.
11. Development prefix reuse is not fresh confirmation; any confirmation requires a newly verified full300 prefix. Frozen runners, data, environments, queues, services, and live runs remain untouched; toy/mock tests are routing/integrity evidence only.
12. The primary outcome is task-level pass over all 300 tasks. Pre-register two-sided exact paired discordance tests for O−E and O−D, with Holm correction across the two comparisons; report net rescue/harm and absolute percentage-point differences. The active-overlap subset is diagnostic only and never changes the denominator. Report actual tokens/latency and cap-matched differences; development-prefix reuse is not a confirmation experiment. Do not change statistical direction or denominator after seeing results.
